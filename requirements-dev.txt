# Development requirements (without hardware dependencies)
# Use this file for local macOS/Windows development

# FastAPI web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Database ORM
sqlalchemy==2.0.45  # ⬆️ Upgraded for Python 3.14 compatibility

# Templates and forms
jinja2==3.1.3
python-multipart==0.0.6

# HTTP requests (for Telegram)
requests==2.31.0

# Environment variables
python-dotenv==1.0.0

# Authentication
bcrypt==4.1.2
python-jose[cryptography]==3.3.0

# Rate limiting
slowapi==0.1.9

# Timezone
pytz==2024.1

# NOTE: evdev and RPi.GPIO are excluded (Linux-only hardware dependencies)
# Use DEV_MODE=true in .env to bypass hardware requirements
